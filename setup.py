import os
from setuptools import find_packages, setup


def get_readme() -> str:
  pass


def get_dependencies() -> list:
  pass


name = "warfle"
version = "0.0.1"
packages = find_packages()


setup = (
  name = name,
  version = version,
  packages = packages
)